#include <stdio.h>

/**
 * main - main function prints the string of word
 *
 * Return:Always 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
	/* return 0 when successful otherwise 1*/
}
